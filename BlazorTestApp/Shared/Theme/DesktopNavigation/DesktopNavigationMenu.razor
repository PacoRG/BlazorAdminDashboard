<div class="coms-desktop-navigation-menu">
    <nav>
        <ul>
            @foreach (var item in Menus)
            {
                <MenuTree Menu="@item"></MenuTree>
            }
        </ul>
    </nav>

</div>

@code {

    [Parameter]
    private List<NavigationMenu> Menus { get; set; }
}
